import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.meinname.myworkoutapp',
  appName: 'myWorkoutApp',
  webDir: 'build'
};

export default config;
